{
  "name": "caresync-platform",
  "version": "1.0.0",
  "description": "CareSync – Smart Healthcare Appointment Platform",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix client install",
    "install-all": "npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build",
    "start": "node server/server.js",
    "dev": "node server/server.js",
    "seed": "npm --prefix server run seed"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.1"
  }
}
